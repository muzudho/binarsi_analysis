* 対局エンジンから対局コロシアムへの移行
    * [x] selfmatch コマンドを、対局エンジンから対局コロシアムへ移行したい
    * [x] play コマンドは廃止したい（コロシアム全体が play の代わりをする）
    * [x] board コマンドを、対局エンジンから対局コロシアムへ移行したい
    * [x] test_board コマンドを、対局エンジンから対局コロシアムへ移行したい
    * [x] do コマンドは、盤表示、SFEN表示はせず、 `do ok` だけ表示するように変更したい
    * [ ] do コマンドのクリアーターゲットの新規作成も不要になるようにしたい
    * [x] clear_targets コマンドを、対局エンジンから対局コロシアムへ移行したい
    * [x] legal_moves コマンドを、対局エンジンから対局コロシアムへ移行したい
    * [x] mate1 コマンドを、対局エンジンから対局コロシアムへ移行したい    
    * [x] moves_for_edit コマンドを、対局エンジンから対局コロシアムへ移行したい    
    * [x] history コマンドを、対局エンジンから対局コロシアムへ移行したい
    * [x] inverse_move コマンドを、対局エンジンから対局コロシアムへ移行したい
* 対局コロシアム
    * [x] タイトル画面を作りたい
    * [ ] 罫線も引ける盤表示をしたい
    * [x] sfen コマンドを付けたい
* 対局エンジン
    * ゲームサイズ
        * 横縦 7x6
            * ランダムムーブ同士の対決だと 黒 15%、白 15%、無 70% ぐらいの勝率
            * 自分の色の石が多くなるように打ち続けると 黒 30%、白 28%、 無 40% ぐらいの勝率。白が有利になるばかり
    * 勝敗判定
        * [x] 黒白が同時にクリアーターゲットを揃えた場合、その盤面で点数勝負へ移行したい
        * [x] 点数勝負のときの計算を差分計算を用いて高速にしたい
    * cshogi との違い
        * [x] is_gameover() は board ではなく、 search に持たせたい
        * [x] legal_moves は、Board が所有するのではなく、 SearchLegalMoves が合法手を生成するようにしたい
        * [x] mate_move_in_1ply() は cshogi のように board に持たせるのではなく、それ専用のクラスが生成するようにしたい
    * 探索
        * [ ] 探索中にクリアーターゲットを獲得して、アンドゥしたとき、クリアーターゲットがアンドゥされてない？
    * 指し手
        * [x] 左右反転や、上下反転したとき、盤面が変わらないような指し手は禁じ手にしたい
        * [ ] 一手詰めを実装したい
            * [ ] mate1 コマンドが機能してない
    * 対局
        * [x] do や go とは別に、相手番でコンピューターが打ち返してくれるような対局ツール（コロシアム）が欲しい
    * クリアーターゲット
        * [x] 状況を表示するコマンドが欲しい clear_targets
    * 棋譜の都合
        * 千日手になりにくいようにしたい
        * 手待ち行為を減らしたい
    * `selfmatch` での作りこみ
        * 棋譜にしたい。
        * 盤に何手目かも表示したい
    * [x] Lock を用意したい（千日手防止）
    * [x] position
        * [x] SFENの仕様作成
        * [x] moves の部分を作りたい
    * [x] 裏返す前の石の状態を記憶したい。将棋でいうところの captured みたいな
    * クリアー演算子も、どこでも消せるのではなく、矩形の辺だけを消せるように制限したい
        * [x] clear を cut the edge に名称変更したい
    * SFEN
        * [x] 何手目表示が添付盤面を指してないので修正したい
        * [x] as_sfen() に引数を追加したい。現局面からのSFENは from_present=True、そうでなければ初期局面からのSFEN
        * [x] クリアー済ターゲットの情報が必要
        * [ ] コミを含めておくか？ 黒と白のどちらにいくつ付けるか定まってないので
    * Move
        * [x] Move の _is_way_unlock 変数と、 Operator の _force_unlock 変数が被ってる。 Move の方だけにするのが自然では？
    * 演算子の実装
        * [ ] 演算子 edit 追加
        * （段階１）セルフマッチ（プレイアウト）ができる程度
            * [-] c
            * [x] s1
            * [x] s2
            * [x] s3
            * [x] s4
            * [x] s5
            * [x] s6
            * [x] n
            * [x] nL
            * [x] nH
            * [-] ze
            * [x] no
            * [x] xo
            * [x] na
            * [x] a
            * [x] xn
            * [x] o
            * [-] on
    * テスト
        * [x] test コマンドの用意
        * [ ] 合法手のテスト